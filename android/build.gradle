buildscript {
    repositories {
        google()  // Ujistěte se, že máte tuto řádku pro přístup k Google Services
        mavenCentral()
    }
    dependencies {
        // Zde přidáme classpath pro Firebase a Google Services
        classpath 'com.android.tools.build:gradle:7.3.1'  // Použijte verzi, která je kompatibilní s vaším projektem
        classpath 'com.google.gms:google-services:4.3.15'  // Verze Google Services pluginu
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.buildDir = "../build"
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(":app")
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}


